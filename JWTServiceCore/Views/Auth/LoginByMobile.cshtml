@model JWTServiceCore.Models.ViewModels.LoginWithPhoneViewModel

@{
    ViewData["Title"] = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Login</h1>

<h4>Login with Phone</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-controller="Auth" asp-action="LoginByMobile">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group mb-2">
                <label asp-for="Phone" class="control-label"></label>
                <input asp-for="Phone" class="form-control" />
                <span asp-validation-for="Phone" class="text-danger"></span>
            </div>
            <div class="form-group form-check mb-2">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="RememberMe" /> @Html.DisplayNameFor(model => model.RememberMe)
                </label>
            </div>
            <!--captcha-->
            <div class="form-group mb-2">
                <google-re-captcha></google-re-captcha>
                @Html.AntiForgeryToken()
            </div>
            <input type="hidden" asp-for="ReturnUrl" />
            <div class="form-group mb-2">
                <input type="submit" value="Login" class="btn btn-primary mt-2" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-controller="Auth" asp-action="Login">Login</a>
</div>

@section Scripts
{
    <script src="https://www.google.com/recaptcha/api.js?hl=fa" async defer></script>
}